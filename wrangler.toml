name = "ascend"
compatibility_date = "2024-01-01"

# Static landing page — no build step. index.html/docs are served directly;
# mobile-app/ (the Android project) and .github/ are excluded via
# .assetsignore since they aren't part of the deployed site.
[assets]
directory = "."
not_found_handling = "404-page"
